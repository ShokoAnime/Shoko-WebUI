/**
 * Generated by orval from Shoko Server's OpenAPI spec (API v3.0).
 * Do not edit manually — run `pnpm orval` to regenerate.
 * Generated at: 2026-07-26T14:29:02.231Z
 * Shoko Server version at generation time: 6.0.0-dev.389
 */
import * as zod from 'zod';

import { APIv3LoggingLogFile, APIv3LoggingLogReadResult } from '../shokoServerAPI30.schemas';

/**
 * @summary Get the ID of the currently running process.
 */
export const GetLoggingProcessIDResponse = zod.int();

/**
 * **Log filter DSL** (`logger`, `caller`, `message`, `exception` query parameters):
 *
 * - If the value has no **':'** in the string, it is shorthand for `c:<entire value>` (substring, case-sensitive).
 * - Otherwise the first character is the **mode**: `c` contains, `=` equals, `^` starts with, `$` ends with, `~` fuzzy (always case-insensitive), `*` regex.
 * - After the mode and before the first `:`: optional `!` (negate) and optional `#` (case-insensitive for `c`, `=`, `^`, `$`), each at most once, in either order. `#` is ignored for `~` and `*`.
 * - **Payload** is everything after the first `:` only (e.g. `c:^:foo` contains the substring `^:foo`).
 * - **Regex** (`*:`): raw pattern or `/pattern/flags`; default case-sensitive; flag `i` = ignore case.
 * - **Exception** query param: entry exception text is compared as `null` coalesced to `""`. `=:` matches lines with no exception text; `=!:` matches lines with non-empty exception text.
 * - A filter is **inactive** when the query parameter is omitted; empty or whitespace-only values are still valid DSL.
 *
 * Plugin authors: same strings as Shoko.Abstractions.Logging.Models.LogBaseOptions filter properties.
 * @summary Read log entries across files filtered by a date range.
 */
export const getLoggingRangeReadQueryOffsetDefault = 0;
export const getLoggingRangeReadQueryOffsetMin = 0;
export const getLoggingRangeReadQueryOffsetMax = 2147483647;

export const getLoggingRangeReadQueryLimitDefault = 100;
export const getLoggingRangeReadQueryLimitMin = 0;
export const getLoggingRangeReadQueryLimitMax = 1000;

export const getLoggingRangeReadQueryDescendingDefault = true;

export const GetLoggingRangeReadQueryParams = zod.object({
  from: zod.iso.datetime({ offset: true }).optional().describe('Optional start date (inclusive).'),
  to: zod.iso.datetime({ offset: true }).optional().describe('Optional end date (inclusive).'),
  offset: zod.int().min(getLoggingRangeReadQueryOffsetMin).max(getLoggingRangeReadQueryOffsetMax).default(
    getLoggingRangeReadQueryOffsetDefault,
  ).describe('Line offset to start from.'),
  limit: zod.int().min(getLoggingRangeReadQueryLimitMin).max(getLoggingRangeReadQueryLimitMax).default(
    getLoggingRangeReadQueryLimitDefault,
  ).describe('Maximum number of entries to return.'),
  descending: zod.boolean().default(getLoggingRangeReadQueryDescendingDefault).describe(
    'Set to `true` for newest-first ordering. Defaults to `true`.',
  ),
  level: zod.string().optional().describe(
    'Comma-separated Microsoft.Extensions.Logging.LogLevel names to include (e.g. `Error,Warning`).',
  ),
  logger: zod.string().optional().describe('Optional DSL filter on logger name; inactive when omitted. See remarks.'),
  caller: zod.string().optional().describe('Optional DSL filter on caller; inactive when omitted. See remarks.'),
  message: zod.string().optional().describe('Optional DSL filter on message; inactive when omitted. See remarks.'),
  exception: zod.string().optional().describe(
    'Optional DSL filter on exception text (`entry.Exception ?? ""`); inactive when omitted. See remarks.',
  ),
  processId: zod.int().optional().describe('Exact process id.'),
  threadId: zod.int().optional().describe('Exact thread id.'),
});

export const GetLoggingRangeReadResponse = APIv3LoggingLogReadResult;

/**
 * **Log filter DSL** (`logger`, `caller`, `message`, `exception` query parameters):
 *
 * - If the value has no **':'** in the string, it is shorthand for `c:<entire value>` (substring, case-sensitive).
 * - Otherwise the first character is the **mode**: `c` contains, `=` equals, `^` starts with, `$` ends with, `~` fuzzy (always case-insensitive), `*` regex.
 * - After the mode and before the first `:`: optional `!` (negate) and optional `#` (case-insensitive for `c`, `=`, `^`, `$`), each at most once, in either order. `#` is ignored for `~` and `*`.
 * - **Payload** is everything after the first `:` only (e.g. `c:^:foo` contains the substring `^:foo`).
 * - **Regex** (`*:`): raw pattern or `/pattern/flags`; default case-sensitive; flag `i` = ignore case.
 * - **Exception** query param: entry exception text is compared as `null` coalesced to `""`. `=:` matches lines with no exception text; `=!:` matches lines with non-empty exception text.
 * - A filter is **inactive** when the query parameter is omitted; empty or whitespace-only values are still valid DSL.
 *
 * Plugin authors: same strings as Shoko.Abstractions.Logging.Models.LogBaseOptions filter properties.
 * @summary Download log entries across files filtered by a date range.
 */
export const GetLoggingRangeDownloadQueryParams = zod.object({
  from: zod.iso.datetime({ offset: true }).optional().describe('Optional start date (inclusive).'),
  to: zod.iso.datetime({ offset: true }).optional().describe('Optional end date (inclusive).'),
  format: zod.enum(['Simple', 'Full', 'Json', 'Legacy', 'Console']).optional().describe(
    'Format to return the log entries as.',
  ),
  level: zod.string().optional().describe('Comma-separated Microsoft.Extensions.Logging.LogLevel names to include.'),
  logger: zod.string().optional().describe('Optional DSL filter on logger; inactive when omitted. See remarks.'),
  caller: zod.string().optional().describe('Optional DSL filter on caller; inactive when omitted. See remarks.'),
  message: zod.string().optional().describe('Optional DSL filter on message; inactive when omitted. See remarks.'),
  exception: zod.string().optional().describe(
    'Optional DSL filter on exception text; inactive when omitted. See remarks.',
  ),
  processId: zod.int().optional().describe('Exact process id.'),
  threadId: zod.int().optional().describe('Exact thread id.'),
});

export const GetLoggingRangeDownloadResponse = zod.unknown();

/**
 * @summary List all available log files.
 */
export const GetLoggingFileResponseItem = APIv3LoggingLogFile;
export const GetLoggingFileResponse = zod.array(GetLoggingFileResponseItem);

/**
 * @summary Get the current log file.
 */
export const GetLoggingFileCurrentResponse = APIv3LoggingLogFile;

/**
 * **Log filter DSL** (`logger`, `caller`, `message`, `exception` query parameters):
 *
 * - If the value has no **':'** in the string, it is shorthand for `c:<entire value>` (substring, case-sensitive).
 * - Otherwise the first character is the **mode**: `c` contains, `=` equals, `^` starts with, `$` ends with, `~` fuzzy (always case-insensitive), `*` regex.
 * - After the mode and before the first `:`: optional `!` (negate) and optional `#` (case-insensitive for `c`, `=`, `^`, `$`), each at most once, in either order. `#` is ignored for `~` and `*`.
 * - **Payload** is everything after the first `:` only (e.g. `c:^:foo` contains the substring `^:foo`).
 * - **Regex** (`*:`): raw pattern or `/pattern/flags`; default case-sensitive; flag `i` = ignore case.
 * - **Exception** query param: entry exception text is compared as `null` coalesced to `""`. `=:` matches lines with no exception text; `=!:` matches lines with non-empty exception text.
 * - A filter is **inactive** when the query parameter is omitted; empty or whitespace-only values are still valid DSL.
 *
 * Plugin authors: same strings as Shoko.Abstractions.Logging.Models.LogBaseOptions filter properties.
 * @summary Read the current log file using line-based paging.
 */
export const getLoggingFileCurrentReadQueryOffsetDefault = 0;
export const getLoggingFileCurrentReadQueryOffsetMin = 0;
export const getLoggingFileCurrentReadQueryOffsetMax = 2147483647;

export const getLoggingFileCurrentReadQueryLimitDefault = 100;
export const getLoggingFileCurrentReadQueryLimitMin = 0;
export const getLoggingFileCurrentReadQueryLimitMax = 1000;

export const getLoggingFileCurrentReadQueryDescendingDefault = false;

export const GetLoggingFileCurrentReadQueryParams = zod.object({
  from: zod.iso.datetime({ offset: true }).optional().describe('Optional start date (inclusive, UTC).'),
  to: zod.iso.datetime({ offset: true }).optional().describe('Optional end date (inclusive, UTC).'),
  offset: zod.int().min(getLoggingFileCurrentReadQueryOffsetMin).max(getLoggingFileCurrentReadQueryOffsetMax).default(
    getLoggingFileCurrentReadQueryOffsetDefault,
  ).describe('Line offset to start from.'),
  limit: zod.int().min(getLoggingFileCurrentReadQueryLimitMin).max(getLoggingFileCurrentReadQueryLimitMax).default(
    getLoggingFileCurrentReadQueryLimitDefault,
  ).describe('Maximum number of entries to return.'),
  descending: zod.boolean().default(getLoggingFileCurrentReadQueryDescendingDefault).describe(
    'If `true`, return newest entries first.',
  ),
  level: zod.string().optional().describe('Comma-separated Microsoft.Extensions.Logging.LogLevel names to include.'),
  logger: zod.string().optional().describe('Optional DSL filter on logger; inactive when omitted. See remarks.'),
  caller: zod.string().optional().describe('Optional DSL filter on caller; inactive when omitted. See remarks.'),
  message: zod.string().optional().describe('Optional DSL filter on message; inactive when omitted. See remarks.'),
  exception: zod.string().optional().describe(
    'Optional DSL filter on exception text; inactive when omitted. See remarks.',
  ),
  processId: zod.int().optional().describe('Exact process id.'),
  threadId: zod.int().optional().describe('Exact thread id.'),
});

export const GetLoggingFileCurrentReadResponse = APIv3LoggingLogReadResult;

/**
 * **Log filter DSL** (`logger`, `caller`, `message`, `exception` query parameters):
 *
 * - If the value has no **':'** in the string, it is shorthand for `c:<entire value>` (substring, case-sensitive).
 * - Otherwise the first character is the **mode**: `c` contains, `=` equals, `^` starts with, `$` ends with, `~` fuzzy (always case-insensitive), `*` regex.
 * - After the mode and before the first `:`: optional `!` (negate) and optional `#` (case-insensitive for `c`, `=`, `^`, `$`), each at most once, in either order. `#` is ignored for `~` and `*`.
 * - **Payload** is everything after the first `:` only (e.g. `c:^:foo` contains the substring `^:foo`).
 * - **Regex** (`*:`): raw pattern or `/pattern/flags`; default case-sensitive; flag `i` = ignore case.
 * - **Exception** query param: entry exception text is compared as `null` coalesced to `""`. `=:` matches lines with no exception text; `=!:` matches lines with non-empty exception text.
 * - A filter is **inactive** when the query parameter is omitted; empty or whitespace-only values are still valid DSL.
 *
 * Plugin authors: same strings as Shoko.Abstractions.Logging.Models.LogBaseOptions filter properties.
 * @summary Download the current log file.
 */
export const GetLoggingFileCurrentDownloadQueryParams = zod.object({
  from: zod.iso.datetime({ offset: true }).optional().describe('Optional start date (inclusive, UTC).'),
  to: zod.iso.datetime({ offset: true }).optional().describe('Optional end date (inclusive, UTC).'),
  format: zod.enum(['Simple', 'Full', 'Json', 'Legacy', 'Console']).optional().describe(
    'Format to return the log entries as.',
  ),
  level: zod.string().optional().describe('Comma-separated Microsoft.Extensions.Logging.LogLevel names to include.'),
  logger: zod.string().optional().describe('Optional DSL filter on logger; inactive when omitted. See remarks.'),
  caller: zod.string().optional().describe('Optional DSL filter on caller; inactive when omitted. See remarks.'),
  message: zod.string().optional().describe('Optional DSL filter on message; inactive when omitted. See remarks.'),
  exception: zod.string().optional().describe(
    'Optional DSL filter on exception text; inactive when omitted. See remarks.',
  ),
  processId: zod.int().optional().describe('Exact process id.'),
  threadId: zod.int().optional().describe('Exact thread id.'),
});

export const GetLoggingFileCurrentDownloadResponse = zod.instanceof(File);

/**
 * @summary Get a specific log file by identifier.
 */
export const GetLoggingFileFileIDParams = zod.object({
  fileID: zod.uuid().describe('Log file identifier.'),
});

export const GetLoggingFileFileIDResponse = APIv3LoggingLogFile;

/**
 * @summary Delete a specific log file.
 */
export const DeleteLoggingFileFileIDParams = zod.object({
  fileID: zod.uuid().describe('Log file identifier.'),
});

export const DeleteLoggingFileFileIDResponse = zod.unknown();

/**
 * **Log filter DSL** (`logger`, `caller`, `message`, `exception` query parameters):
 *
 * - If the value has no **':'** in the string, it is shorthand for `c:<entire value>` (substring, case-sensitive).
 * - Otherwise the first character is the **mode**: `c` contains, `=` equals, `^` starts with, `$` ends with, `~` fuzzy (always case-insensitive), `*` regex.
 * - After the mode and before the first `:`: optional `!` (negate) and optional `#` (case-insensitive for `c`, `=`, `^`, `$`), each at most once, in either order. `#` is ignored for `~` and `*`.
 * - **Payload** is everything after the first `:` only (e.g. `c:^:foo` contains the substring `^:foo`).
 * - **Regex** (`*:`): raw pattern or `/pattern/flags`; default case-sensitive; flag `i` = ignore case.
 * - **Exception** query param: entry exception text is compared as `null` coalesced to `""`. `=:` matches lines with no exception text; `=!:` matches lines with non-empty exception text.
 * - A filter is **inactive** when the query parameter is omitted; empty or whitespace-only values are still valid DSL.
 *
 * Plugin authors: same strings as Shoko.Abstractions.Logging.Models.LogBaseOptions filter properties.
 * @summary Read a specific log file using line-based paging.
 */
export const GetLoggingFileFileIDReadParams = zod.object({
  fileID: zod.uuid().describe('Log file identifier.'),
});

export const getLoggingFileFileIDReadQueryOffsetDefault = 0;
export const getLoggingFileFileIDReadQueryOffsetMin = 0;
export const getLoggingFileFileIDReadQueryOffsetMax = 2147483647;

export const getLoggingFileFileIDReadQueryLimitDefault = 100;
export const getLoggingFileFileIDReadQueryLimitMin = 0;
export const getLoggingFileFileIDReadQueryLimitMax = 1000;

export const getLoggingFileFileIDReadQueryDescendingDefault = false;

export const GetLoggingFileFileIDReadQueryParams = zod.object({
  from: zod.iso.datetime({ offset: true }).optional().describe('Optional start date (inclusive, UTC).'),
  to: zod.iso.datetime({ offset: true }).optional().describe('Optional end date (inclusive, UTC).'),
  offset: zod.int().min(getLoggingFileFileIDReadQueryOffsetMin).max(getLoggingFileFileIDReadQueryOffsetMax).default(
    getLoggingFileFileIDReadQueryOffsetDefault,
  ).describe('Line offset to start from.'),
  limit: zod.int().min(getLoggingFileFileIDReadQueryLimitMin).max(getLoggingFileFileIDReadQueryLimitMax).default(
    getLoggingFileFileIDReadQueryLimitDefault,
  ).describe('Maximum number of entries to return.'),
  descending: zod.boolean().default(getLoggingFileFileIDReadQueryDescendingDefault).describe(
    'If `true`, return newest entries first.',
  ),
  level: zod.string().optional().describe('Comma-separated Microsoft.Extensions.Logging.LogLevel names to include.'),
  logger: zod.string().optional().describe('Optional DSL filter on logger; inactive when omitted. See remarks.'),
  caller: zod.string().optional().describe('Optional DSL filter on caller; inactive when omitted. See remarks.'),
  message: zod.string().optional().describe('Optional DSL filter on message; inactive when omitted. See remarks.'),
  exception: zod.string().optional().describe(
    'Optional DSL filter on exception text; inactive when omitted. See remarks.',
  ),
  processId: zod.int().optional().describe('Exact process id.'),
  threadId: zod.int().optional().describe('Exact thread id.'),
});

export const GetLoggingFileFileIDReadResponse = APIv3LoggingLogReadResult;

/**
 * **Log filter DSL** (`logger`, `caller`, `message`, `exception` query parameters):
 *
 * - If the value has no **':'** in the string, it is shorthand for `c:<entire value>` (substring, case-sensitive).
 * - Otherwise the first character is the **mode**: `c` contains, `=` equals, `^` starts with, `$` ends with, `~` fuzzy (always case-insensitive), `*` regex.
 * - After the mode and before the first `:`: optional `!` (negate) and optional `#` (case-insensitive for `c`, `=`, `^`, `$`), each at most once, in either order. `#` is ignored for `~` and `*`.
 * - **Payload** is everything after the first `:` only (e.g. `c:^:foo` contains the substring `^:foo`).
 * - **Regex** (`*:`): raw pattern or `/pattern/flags`; default case-sensitive; flag `i` = ignore case.
 * - **Exception** query param: entry exception text is compared as `null` coalesced to `""`. `=:` matches lines with no exception text; `=!:` matches lines with non-empty exception text.
 * - A filter is **inactive** when the query parameter is omitted; empty or whitespace-only values are still valid DSL.
 *
 * Plugin authors: same strings as Shoko.Abstractions.Logging.Models.LogBaseOptions filter properties.
 * @summary Download a specific log file.
 */
export const GetLoggingFileFileIDDownloadParams = zod.object({
  fileID: zod.uuid().describe('Log file identifier.'),
});

export const GetLoggingFileFileIDDownloadQueryParams = zod.object({
  from: zod.iso.datetime({ offset: true }).optional().describe('Optional start date (inclusive, UTC).'),
  to: zod.iso.datetime({ offset: true }).optional().describe('Optional end date (inclusive, UTC).'),
  format: zod.enum(['Simple', 'Full', 'Json', 'Legacy', 'Console']).optional().describe(
    'Format to return the log entries as.',
  ),
  level: zod.string().optional().describe('Comma-separated Microsoft.Extensions.Logging.LogLevel names to include.'),
  logger: zod.string().optional().describe('Optional DSL filter on logger; inactive when omitted. See remarks.'),
  caller: zod.string().optional().describe('Optional DSL filter on caller; inactive when omitted. See remarks.'),
  message: zod.string().optional().describe('Optional DSL filter on message; inactive when omitted. See remarks.'),
  exception: zod.string().optional().describe(
    'Optional DSL filter on exception text; inactive when omitted. See remarks.',
  ),
  processId: zod.int().optional().describe('Exact process id.'),
  threadId: zod.int().optional().describe('Exact thread id.'),
});

export const GetLoggingFileFileIDDownloadResponse = zod.instanceof(File);

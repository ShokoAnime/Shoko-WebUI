// @flow
import { createAction } from 'redux-actions';

export const SETTINGS_QUICK_ACTION_SET = 'SETTINGS_QUICK_ACTION_SET';
export const settingsQuickActions = createAction(SETTINGS_QUICK_ACTION_SET);
